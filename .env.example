# Anthropic API key — get one at https://console.anthropic.com/
ANTHROPIC_API_KEY=

# GitHub personal access token (used to fetch PR diffs from private repos)
# https://github.com/settings/tokens — needs `repo` scope for private repos
GITHUB_TOKEN=
